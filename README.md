# m10c-content-elements

CMS content elements bundle (block editor, SEO, variants) for m10c admin projects. Pairs with `m10c-mui-kit` for the underlying form/theme primitives.

## Setup

```bash
yarn add m10c-content-elements
```
